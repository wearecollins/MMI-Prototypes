{
  "name": "fetchtoken",
  "version": "1.0.0",
  "description": "fetches a Facebook Page access token on behalf of a given user.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js ./config.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.0",
    "express": "^4.13.4",
    "fb": "^1.0.2"
  }
}
