<!-- 
	data: {
		name: string (character name),
		img:  string (image)
	}
 -->
<div id="wrapper">
	<div class="title" class="top">
	</div>
	<div id="capture" class="middle">
	{{#if didInteract }}
		<img src="{{img}}" />
	{{/if}}
	<div id="Text">
		<strong>Please Turn Puppet Around</strong> and<br>
		<strong>Return Features to Drawers</strong>
	</div>
	{{#if didInteract }}
		<img src="{{img_original}}" />
	{{/if}}
	</div>
</div>